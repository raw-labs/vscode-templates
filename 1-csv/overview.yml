raw: 0.9
endpoint: GET
metadata:
  title: Airports per country
  description: List with number of airports per country.
  tags:
    - airports
code: rql
declaration: number_airports_per_country
codeFile: code.rql
format: json
security:
  public: true
computeClass: normal
enabled: true
